# Environment Variables for Backend Deployment

# Server Configuration
PORT=5000
NODE_ENV=production

# Gold Price API (Optional - app will use fallback if not provided)
GOLD_API_KEY=your_gold_api_key_here

# CORS Configuration (Add your frontend domain)
FRONTEND_URL=https://your-frontend-domain.vercel.app 